{
  "name": "smartseason",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
